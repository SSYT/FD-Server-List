(function(jQuery, {}) {
	if(/\/live_stats=/g.test(window.location.pathname))
	{
		jQuery('ol.breadcrumb.float-xs-right.nav_breadcrumb_top_align li.breadcrumb-item.active').text('Live Stats');
		jQuery('#sample_1_wrapper').hide();
		jQuery('#sample_2_wrapper').show();
		jQuery('.card-header.bg-white').html('<i class="fa fa-table"></i> Server Stats');
	}
}(jQuery, window));